repositories {
    mavenCentral()
}

dependencies {
    compile project(':model')
    testCompile 'junit:junit:4.11'
}

test {
    testLogging {
        events 'started', 'passed', 'skipped', 'failed'
    }
}